串行GC
  年轻代年老代都是单线程GC
  效率低而且高延迟，适合几百兆的内存使用（因为内存这么小，大家的性能拉不开差距）；
并行GC
  年轻代并行parnew年老代并发标记清除整理
  吞吐量最高，垃圾回收效率最高；适合注重吞吐量，允许高延迟的场景；
CMS
  年轻代parnew年老代（初始标记stw，并发标记，并发预清理，最终标记stw，并发清理，并发重置）并发标记清除；
  注重吞吐量和延迟；缺点是有碎片产生
G1
  内存在逻辑上分成小块，所有年轻代的小块组成年轻代，老年代同理；年轻代是标记复制算法，年老代是标记清除整理算法
  stw发生在初始标记阶段；
  参数：IHOP老年代多少的时候启动回收；-XX:MaxGCPauseMills GC最大停顿时间；-XX：+GCTimeRatio花在gc上的时间比率；
  优化了cms的碎片问题，GC时间可控；


